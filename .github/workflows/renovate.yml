name: Renovate
on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches: [main]
  workflow_dispatch:
concurrency:
  group: renovate
jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          client-id: ${{ secrets.RENOVATE_APP_CLIENT_ID }}
          private-key: ${{ secrets.RENOVATE_APP_PRIVATE_KEY }}
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      # Renovate puts this date into the branch name and opens one lock file maintenance PR per day.
      # `branchTopic` does not work here: with grouping on, Renovate replaces it with the group slug.
      - name: Build the dated Renovate config
        run: |
          jq -n --arg date "$(date -u +%Y-%m-%d)" '{
            lockFileMaintenance: {
              additionalBranchPrefix: "\($date)-",
              commitMessageAction: "Lock file maintenance (\($date))"
            }
          }' > renovate-dated-config.json
      - uses: renovatebot/github-action@c314dda4a9a93b08239ec978f2043b9a433b9ac4 # v46.3.0
        with:
          token: ${{ steps.app-token.outputs.token }}
          renovate-version: 44.65.5
          configurationFile: renovate-dated-config.json
        env:
          RENOVATE_REPOSITORIES: ${{ github.repository }}
