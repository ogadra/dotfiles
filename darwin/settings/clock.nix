{ ... }:
{
  system.defaults.menuExtraClock = {
    # 24時間表示
    Show24Hour = true;

    # 秒を表示
    ShowSeconds = true;

    # 曜日を表示
    ShowDayOfWeek = true;

    # アナログ表示にしない
    IsAnalog = false;
  };
}
