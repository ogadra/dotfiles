{ ... }:
{
  system.defaults.menuExtraClock = {
    # 24時間表示
    Show24Hour = true;

    # 秒を表示
    ShowSeconds = true;

    # 曜日を表示
    ShowDayOfWeek = true;

    # アナログ表示にしない
    IsAnalog = false;

    # 日付の表示
    # 0=スペースがある時, 1=常に表示, 2=表示しない
    ShowDate = 1;
  };
}
