{ ... }:
{
  system.defaults.menuExtraClock = {
    Show24Hour = true;
    ShowSeconds = true;
    ShowDayOfWeek = true;
    IsAnalog = false;

    # 0=when there is room, 1=always, 2=never
    ShowDate = 1;
  };
}
